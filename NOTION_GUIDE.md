# AutoRace Notion 정리 기준

## 목적

- AutoRace의 진행 현황과 학습 내용을 서로 다른 문서 유형으로 관리한다.
- 학습은 `개념 → 최소 예제 → 퀴즈 → 내 말로 설명 → 현재 코드 → 한국어 주석 → 오답 복습` 순서로 진행한다.
- 처음 보는 사람도 이해할 수 있는 설명을 남기면서 실제 저장소 구현까지 연결한다.
- 기존에 같은 주제의 페이지가 있으면 새 페이지를 만들지 않고 새 구조로 교체한다.

## 공통 원칙

- 코드, 설정, 실행 결과와 사용자가 말한 상태처럼 확인된 사실만 기록한다.
- 작성자가 성과, 우선순위, 병목이나 완료 근거를 임의로 판단해 추가하지 않는다.
- 이해에 필요한 개념, 예제와 코드 설명을 짧게 줄인다는 이유로 삭제하지 않는다.
- 긴 줄글 대신 탭, 표, 번호 목록, 체크리스트와 토글을 사용한다.
- 한 문단은 하나의 내용만 다루고 가능하면 3줄을 넘기지 않는다.
- 문제 해결 과정이나 수정 이력보다 현재 코드가 무엇을 어떻게 구현하는지 먼저 설명한다.
- 문제 원인과 수정 이력은 사용자가 요청한 경우에만 별도 영역으로 만든다.
- 명령어, 코드, 토픽, 파라미터와 파일 경로는 일반 설명과 구분해 코드 형식으로 표시한다.

## 문서 유형

| 문서 | 목적 | 적용 구조 |
|---|---|---|
| 진행 현황 | 프로젝트에서 끝난 일과 다음에 할 일 확인 | `완료`, `진행 예정` 탭 |
| 학습 허브 | 학습 주제, 순서와 이해 상태 확인 | 카테고리별 주제 목록 |
| 주제별 학습 페이지 | 개념부터 현재 코드까지 단계적으로 학습 | 고정된 7개 학습 탭 |

AutoRace 메인 허브 한 페이지에 진행 현황과 학습 허브를 함께 둘 수 있다. 이 경우 최상단을 `완료`, `진행 예정`, `학습` 탭으로 나누되 각 탭의 내용은 섞지 않는다. 주제별 학습 페이지를 완료·진행 예정 체크리스트로 축약하지 않는다.

## 진행 현황 페이지

- 진행 상태를 나타내는 탭은 `완료`와 `진행 예정`만 사용한다.
- 같은 메인 허브에 학습 자료를 연결할 때는 별도 `학습` 탭을 추가할 수 있으며, 이를 진행 상태로 취급하지 않는다.
- 각 탭 안에서는 `환경`, `인식`, `위치 추정`, `경로 계획`, `제어`, `미션`, `실물`처럼 관련 항목을 카테고리별로 묶는다.
- 완료된 항목을 진행 예정에 중복해서 넣지 않는다.
- 메인 페이지 항목은 한 줄 체크리스트로 작성한다.
- 상세 설명, 구현 구조, 파라미터와 참고자료는 관련 학습·기술 페이지에 둔다.
- 시험 횟수, 성공률이나 완료 근거는 사용자가 제공했거나 기록을 직접 확인했고 사용자가 필요하다고 한 경우에만 표시한다.

## 학습 허브

### 기본 카테고리

- ROS·환경
- 인식
- 위치 추정
- 경로 계획
- 제어
- 미션
- 실물 하드웨어

주제 성격에 맞지 않는 카테고리는 만들지 않는다.

### 주제별 관리 항목

| 항목 | 기록 내용 |
|---|---|
| 학습 주제 | AMCL, 휴리스틱, Hybrid A*처럼 한 번에 학습할 범위 |
| 카테고리 | 기본 카테고리 중 하나 |
| 학습 상태 | 현재 도달한 학습 단계 |
| 선행 개념 | 먼저 알아야 하는 주제 |
| 관련 코드 | 실제 파일, 함수 또는 노드 |
| 마지막 학습일 | 마지막으로 내용을 확인한 날짜 |
| 다음 복습일 | 오답이나 불확실한 내용이 있을 때만 지정 |

### 학습 상태

`처음 봄 → 개념 이해 → 예제 설명 가능 → 퀴즈 확인 → 내 말로 설명 가능 → 실제 코드 추적 가능 → 주석 작성 가능`

- 단계를 자동으로 올리지 않는다.
- 사용자가 확인했거나 학습 결과로 확인된 단계까지만 표시한다.

## 주제별 학습 페이지

페이지 위에는 다음 정보만 짧게 표시한다.

- 이번에 이해할 내용
- 필요한 선행 개념
- 현재 학습 상태

본문은 아래 7개 탭을 학습 순서대로 사용한다.

### 1. 개념 이해

- 무엇인지, 왜 필요한지, 어떤 문제에 사용하는지 설명한다.
- 입력, 처리와 출력을 먼저 쉬운 말로 구분한다.
- 처음 보는 용어는 바로 아래에서 정의한다.
- 일반적인 개념과 AutoRace에서의 사용 목적을 섞지 않고 나누어 설명한다.
- 이해를 돕는 비유는 실제 동작과 다른 지점을 함께 표시한다.

### 2. 최소 예제

- 개념이 실제로 움직이는 가장 작은 숫자, 상황 또는 코드를 사용한다.
- `입력 → 단계별 처리 → 결과`가 보이도록 작성한다.
- 계산식에는 각 기호의 뜻과 단위를 함께 적는다.
- 코드는 핵심 흐름만 포함하고 실행 결과 또는 예상되는 상태 변화를 함께 설명한다.
- 큰 예제 하나보다 한 개념만 보여 주는 작은 예제를 우선한다.

### 3. 확인 퀴즈

- 핵심 개념, 예제 해석과 잘 헷갈리는 차이를 확인하는 문제를 3~5개 둔다.
- 객관식, OX, 짧은 서술형을 필요한 만큼 섞는다.
- 정답과 해설은 각 문제 아래 토글 안에 숨긴다.
- 정답만 적지 않고 왜 맞는지와 자주 하는 오해를 짧게 설명한다.
- 실제 코드를 보지 않아도 기본 개념을 이해했는지 먼저 확인할 수 있어야 한다.

### 4. 내 말로 설명

- 사용자가 직접 설명할 수 있도록 빈 작성 공간을 둔다.
- 답을 미리 채우기보다 다음과 같은 유도 질문을 제공한다.
  - 이것은 무엇을 해결하는가?
  - 입력이 들어오면 어떤 순서로 처리되는가?
  - 결과는 어디에 사용되는가?
  - 비슷한 개념과 무엇이 다른가?
- 필요한 경우 접힌 `참고 설명`을 두어 작성 후 비교할 수 있게 한다.

### 5. 현재 코드 연결

- 반드시 현재 저장소를 직접 확인한 뒤 작성한다.
- 일반 이론과 현재 프로젝트에서 실제로 구현한 내용을 별도 소제목으로 구분한다.
- 함수 이름만 나열하지 않고 입력이 내부 데이터로 바뀌어 최종 출력으로 이어지는 과정을 설명한다.
- 문제를 해결했던 과정이 아니라 현재 코드의 역할과 동작을 중심으로 작성한다.
- launch와 YAML이 동작에 영향을 주면 코드와 함께 연결해 설명한다.

다음 표의 항목을 필요한 만큼 사용한다.

| 항목 | 설명 |
|---|---|
| 파일·함수 또는 노드 | 구현 위치와 실행 주체 |
| 목적 | 이 코드가 맡은 한 가지 책임 |
| 입력 | 구독 토픽, 함수 인자, 센서값 또는 설정값 |
| 처리 과정 | 입력이 변환되고 판단되는 순서 |
| 출력 | 발행 토픽, 반환값, 서비스 또는 명령 |
| 내부 상태 | 저장하는 값과 상태가 바뀌는 조건 |
| 좌표계·단위 | frame과 거리·각도·시간 단위 |
| 호출 흐름 | 앞에서 호출하는 코드와 다음에 이어지는 코드 |
| 내가 이해한 설명 | 사용자가 한국어로 다시 적는 공간 |

상태 머신이 있으면 아래 형식의 표를 추가한다.

| 현재 상태 | 실행되는 코드 | 다음 상태 조건 | 출력 |
|---|---|---|---|

### 6. 한국어 주석 실습

- 핵심 함수나 처리 흐름을 짧은 코드 단위로 나눈다.
- 먼저 `내가 작성할 주석` 영역을 비워 둔다.
- 그 아래에 정답이 아닌 `참고 해설` 토글을 두어 비교할 수 있게 한다.
- 해설은 문법 번역보다 그 줄의 목적, 입력값 변화와 다음 코드와의 관계를 설명한다.
- 사용자가 별도로 요청하지 않으면 이 연습을 위해 저장소 원본 코드의 주석을 변경하지 않는다.

### 7. 오답·복습

- 틀린 문제, 확실히 설명하지 못한 개념과 코드에서 놓친 부분만 기록한다.
- 이미 이해한 내용을 반복해서 복사하지 않는다.
- 각 항목에는 `헷갈린 내용`, `바른 이해`, `다시 확인할 예제`, `다음 복습일`을 둔다.
- 복습 후 이해가 확인되면 학습 상태를 해당 단계까지 갱신한다.

### 참고자료

- 7개 탭 아래에 강의, 공식 문서, 논문과 원문 링크를 모은다.
- 링크마다 어떤 탭을 이해하는 데 도움이 되는지 한 줄로 적는다.
- 실제로 확인하지 않은 링크나 현재 내용과 직접 관련 없는 자료는 추가하지 않는다.

## 기존 페이지를 새 버전으로 교체하는 규칙

1. Notion에서 같은 제목과 같은 주제를 먼저 검색한다.
2. 기존 페이지를 전체 확인하고 설명, 예제, 퀴즈, 코드, 참고자료와 하위 페이지를 목록화한다.
3. 같은 주제의 페이지가 있으면 새 페이지를 만들지 않고 기존 페이지를 수정한다.
4. 유효한 기존 내용은 다음 기준으로 새 위치에 옮긴다.

| 기존 내용 | 새 위치 |
|---|---|
| 동작 원리, 쉬운 비유, 용어 설명 | `개념 이해` |
| 수치 예시, 의사 코드, 작은 실행 예 | `최소 예제` |
| 스스로 확인할 질문 | `확인 퀴즈` |
| 사용자가 작성한 요약 | `내 말로 설명` |
| 파일, 함수, 노드, 토픽과 파라미터 | `현재 코드 연결` |
| 코드 줄별 설명 | `한국어 주석 실습` |
| 헷갈린 내용과 틀린 문제 | `오답·복습` |
| 강의와 공식 문서 | 페이지 하단 `참고자료` |

5. 새 구조를 기존 본문 아래에 덧붙여 두 버전을 함께 남기지 않는다.
6. 유효한 내용을 모두 옮긴 뒤 중복된 이전 레이아웃을 제거한다.
7. 페이지 URL, 하위 페이지, 데이터베이스, 댓글, 첨부파일과 관련 링크를 유지한다.
8. 오래된 코드 설명은 현재 저장소와 비교해 확인한 뒤 갱신한다.
9. 사실 여부를 확인하지 못한 내용은 임의로 고치거나 삭제하지 않고 `확인 필요`로 구분한다.
10. 수정 후 페이지를 다시 확인해 탭 구조, 내용 누락과 링크 유지 여부를 검수한다.

## 새 페이지 작성 규칙

- 같은 주제의 기존 페이지가 없을 때만 새 페이지를 만든다.
- 학습 허브 또는 가장 가까운 상위 페이지 아래에 만든다.
- 관련 주제는 새 설명을 복사하지 않고 기존 페이지를 연결한다.
- 처음 작성할 때 내용을 알 수 없는 탭은 삭제하지 말고 학습 질문이나 작성 공간만 둔다.

## 최종 검수

- 진행 현황과 학습 문서의 구조가 섞이지 않았는가?
- 7개 학습 탭이 정해진 순서로 있는가?
- 개념을 모르는 사람도 최소 예제까지 따라갈 수 있는가?
- 퀴즈 정답과 해설이 접혀 있는가?
- 실제 코드 설명이 현재 저장소와 일치하는가?
- 코드의 입력, 처리, 출력과 상태 변화가 설명되어 있는가?
- 사용자가 직접 설명하고 주석을 작성할 공간이 있는가?
- 오답·복습에는 아직 불확실한 내용만 있는가?
- 임의의 성과나 완료 판단이 들어가지 않았는가?
- 기존의 유효한 설명, 링크와 하위 페이지가 사라지지 않았는가?
- 같은 주제의 중복 페이지나 이전 레이아웃이 남지 않았는가?
