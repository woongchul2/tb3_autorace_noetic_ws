# Custom AutoRace Project Instructions

## 최종 목표

이 프로젝트의 최종 목표는 고정된 AutoRace 경기장에서 출발한 로봇이 모든 미션을 순서대로 완수하고 결승선을 가능한 한 빠르게 통과하는 것이다.

우선순위는 다음과 같다.

1. 경기 규정 준수
2. 반복 주행에서의 안정성과 재현성
3. 단순한 주행
4. 전체 완주시간 단축

## 코드 변경 판단 기준

모든 코드·launch·YAML·URDF 변경은 다음 질문을 기준으로 검토한다.

- 전체 완주시간을 줄이는가?
- 차선 검출, 신호등·표지판 판독과 미션 성공률을 유지하거나 높이는가?
- 동일한 초기 조건의 전후 시험으로 효과를 확인할 수 있는가?
- 코드의 구성이 복잡하지 않고 단순한가?

## 구현 원칙

- 실제 시험에서 관측되지 않았고 경기 규정에도 없는 낮은 가능성의 상황을 가정해  코드를 생성하지 않는다.
- 원본 내부 기능과 같은 데이터를 다시 계산하는 우회 노드를 추가하지 않는다. 안정적인
  통합에 원본 수정이 필요하면 원본을 수정하고, 하나의 계산 경로와 하나의 책임 주체만 둔다.
- 임시 호환 코드, 사용되지 않는 구형 함수와 중복 토픽 발행자는 최종 구현에 남기지 않는다.
- 조정 가능한 거리·각도·속도·게인·타임아웃·검출 임계값은 가능한 한 YAML 또는 launch 파라미터로 둔다.
- Gazebo 전용 보정값과 실물 D405·Mid-360·OpenCR 보정값을 구분한다.
- `/cmd_vel`을 발행하는 노드 사이에 명확한 제어권 인계가 있어야 한다.
- launch, 토픽, 실행 순서나 사용자 명령이 바뀌면 `DOCKER_NOETIC.md`도 최신 상태로 갱신한다, 단 설명을 기입하지않고 사용하는 명령어와 명령어의 역할만 간단하게 기입한다.

## 검증 기준

변경 전후를 가능한 같은 출발 위치와 조건에서 비교한다.

### 실험 완료 판정

- 특정 미션의 변경을 최종 완료로 판정하려면 Gazebo와 관련 노드를 새로 시작하고,
  경기의 공식 시작 자세에서 통합 launch로 출발해 앞선 미션을 모두 순서대로 거친 뒤
  해당 미션의 진입부터 종료 후 차선 복귀까지 사람의 개입 없이 완수해야 한다.
- 단위 테스트, 정적 기하 검사, 미션 직전 자세에서 시작한 전용 회귀, 이전 미션 비활성화,
  중간 순간이동 또는 gate 수동 발행은 원인 분석과 중간 검증으로만 인정한다. 이 결과만으로
  해당 미션이나 변경이 최종 완료되었다고 보고하지 않는다.
- 공식 시작점 통합 주행을 수행하지 못했거나 목표 미션까지 도달하지 못했다면 완료라고
  표현하지 않고, 실제로 검증한 시작점·활성화한 미션·도달 상태와 남은 통합 검증을 명시한다.
- 프로젝트 전체 완료는 같은 방식으로 공식 시작점부터 모든 미션을 순서대로 완수하고
  결승선을 통과한 경우에만 판정한다.

- 출발부터 결승선까지 총 소요시간
- 각 미션 진입·완료 시간
- 차선 이탈, 충돌, 검출 누락과 실패 횟수
- 반복 주행 성공 여부

## Notion 정리

- AutoRace 관련 Notion 페이지를 만들거나 수정할 때는 `NOTION_GUIDE.md`를 따른다.
